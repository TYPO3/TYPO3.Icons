{% if iconInline %}
{{ icon._inline|raw }}
{% else %}
{% set spriteParts = icon.sprite|split('#') %}
<svg role="img"><use xlink:href="{{ pathPrefix }}dist/{{ spriteParts[0] }}?v={{ cacheBuster }}#{{ spriteParts[1] }}" /></svg>
{% endif %}
